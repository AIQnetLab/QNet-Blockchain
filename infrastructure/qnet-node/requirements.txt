# QNet Core Requirements

# Basic dependencies - core framework
Flask==2.0.1
Werkzeug==2.0.1
gunicorn==20.1.0
requests==2.26.0

# Cryptography - essential packages only
cryptography==36.0.1
pycryptodome==3.10.1
PyJWT==2.1.0
base58==2.1.0
ecdsa==0.17.0

# Storage and database - with fallback options
sortedcontainers==2.4.0
# python-rocksdb is installed conditionally in the install script

# Networking and API - essential only
aiohttp==3.7.4
websockets==10.0

# Monitoring and metrics - optional components
psutil==5.8.0
# prometheus_client is installed if enabled

# Utilities - ensure compatibility with existing code
coloredlogs==15.0.1
markdown==3.3.4
pytz==2021.3
python-dotenv==0.19.2

# Solana Integration (Activation Bridge & Potential Future Uses)
solana==0.30.0
solders==0.15.0
pynacl==1.5.0

# Dev/test dependencies are not included in production requirements