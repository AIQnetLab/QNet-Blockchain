# QNet CLI

Small Python command-line tool for interacting with a QNet node over its HTTP API.

Full documentation: [docs/applications/cli.md](../../docs/applications/cli.md)

## Use

```bash
python qnet_cli.py --help
```

Requires Python 3.8 or newer. The tool targets a node endpoint you supply; it does not embed
any network address.

## Licence

Apache-2.0 (see [LICENSE](LICENSE)). The blockchain node software in the rest of the
repository is licensed separately — see the root [LICENSE](../../LICENSE).
