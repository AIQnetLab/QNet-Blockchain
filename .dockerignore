# Only exclude git and build artifacts
.git
target/
*/target/
*.log

# Keep everything else including core/, docs/, etc
# !core/
# !development/
# !infrastructure/

# Exclude top-level documentation and backups (not core module docs)
./docs/
./documentation/
./backups/
./ARCHIVE/
./testing/
./scripts/
./monitoring/

# Exclude applications (not needed for core node)
./applications/
./governance/
./deployment/

# Exclude markdown files at root level only
/*.md
README.md
CHANGELOG.md

# Keep all core/ and infrastructure/ modules
!core/
!infrastructure/
!development/ 