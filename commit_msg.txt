feat: mainnet readiness — Kademlia DHT, O(k) rollback, HealthPing Dilithium signing, chunked snap sync, contract storage CF, incremental replay, deferred side effects

- Kademlia DHT: full 256 k-bucket routing table, XOR distance, upsert/find_closest/remove, iterative FIND_NODE lookup, periodic refresh task (10 min), FindNode/FindNodeResponse handlers
- HealthPing Dilithium3 signing: sign payload QNET_HEALTH_PING_V1 in health check task, verify_health_ping_signature with pqcrypto_dilithium, anti-replay check age <= 60s
- Rollback Merkle O(k): BlockSnapshot with pre_images + created_keys, record_pre_images before apply_transaction_lazy and apply_gas_refund, rollback_block touches only k modified leaves via remove_lazy/insert_lazy + single finalize
- Chunked snap sync: SnapshotManifest with per-chunk SHA3 hashes, 4MB chunks, parallel download Semaphore(4), hash verification per chunk, manifest and chunk RPC endpoints
- Contract storage RocksDB CF: dedicated contract_storage column family, save/delete_contract_storage, CONTRACT_STORAGE_V1 section in create_state_snapshot and load_state_snapshot, stale data cleanup on empty storage
- Incremental replay on restart: track restored_snapshot_height, replay loop from snapshot+1 to chain_height, apply_transaction_lazy + apply_gas_refund + finalize_merkle + state_root verification per block
- hash_account: include is_contract, contract_code_hash, contract_storage (sorted keys); exclude pending_rewards and reputation
- update_pending_rewards: remove insert_lazy call — no out-of-band Merkle modification
- Deferred side effects: pool3, node_registration, emission_macroblocks, reward_clears execute only after save_microblock Ok
- Rollback on save_microblock failure: match around save_microblock calls rollback_block on Err
- Node registry in snapshots: NODE_REGISTRY_V1 section in create/load_state_snapshot
- Snapshot format: unified SHA3+Zstd+type_byte, atomic WriteBatch, distinct key prefixes
- fsync=true for WAL durability
- Zombie QUIC detection: is_connection_alive checks last_activity_ms with 60s timeout
- Integer safety: saturating_add/sub/mul across node, rpc, storage, p2p
- Fix compile errors: scope fix for set_wallet_identity and start_kademlia_refresh_task, add missing HealthPing signature fields, remove duplicate method definitions, add pqcrypto_traits imports
